{
  "name": "@mrpl/dsh-workbench-rag",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Permission-aware retrieval: authorize chunks before reranking, never letting the LLM see unauthorized text.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": [
    "lib/index.js",
    "lib/**/*.d.ts"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint src/",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@mrpl/dsh-workbench-types": "workspace:*"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "workspace:*"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "workspace:*",
    "vitest": "^3.2.0"
  }
}
